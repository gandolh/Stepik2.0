@page "/crud/lesson/{ViewMode?}";
@using Licenta.UI.Components.Backoffice.Lesson
@inherits BaseCrudPage

<CrudPageLayout>
    @if (string.IsNullOrEmpty(ViewMode))
    {
        <LessonAll />
    }
    else if (ViewMode == "one")
    {
        <h1>one</h1>
    }
    else
    {
        <h1> other </h1>
    }
</CrudPageLayout>